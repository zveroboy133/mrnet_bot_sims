# 🚀 Инструкция по развертыванию на сервере

## 🎯 Быстрый старт (Рекомендуемый)

```bash
# 1. Загрузите код на сервер
# 2. Перейдите в директорию проекта
cd /path/to/find_sims-main

# 3. Проверьте, что установлено на сервере
./check_server.sh

# 4. Запустите универсальный скрипт развертывания
./universal_deploy.sh
```

## Вариант 1: Docker (Рекомендуемый)

### Первоначальная настройка:
```bash
# 1. Установите Docker и Docker Compose
sudo ./install_docker.sh

# 2. Перезагрузитесь или выполните:
newgrp docker

# 3. Запустите приложение
./deploy.sh
```

### Обновление кода:
```bash
# Просто запустите скрипт обновления
./deploy.sh
```

## Вариант 2: Виртуальное окружение

### Первоначальная настройка:
```bash
# 1. Установите Python3 (если не установлен)
sudo apt update && sudo apt install python3 python3-venv python3-pip

# 2. Настройте окружение (только один раз)
./setup_env.sh

# 3. Активируйте окружение и запустите
source find_sims_env/bin/activate
python main.py
```

### Обновление кода:
```bash
# Запустите скрипт обновления
./update_code.sh
```

## 🔧 Устранение проблем

### Проблема: "docker-compose: command not found"
```bash
# Решение 1: Установите Docker Compose
sudo ./install_docker.sh

# Решение 2: Используйте новый формат
docker compose up -d  # вместо docker-compose up -d

# Решение 3: Используйте виртуальное окружение
./setup_env.sh
```

### Проблема: "Permission denied"
```bash
# Добавьте пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker
```

### Проблема: "Python3 не найден"
```bash
# Установите Python3
sudo apt update
sudo apt install python3 python3-venv python3-pip
```

## 📁 Структура файлов для загрузки

При загрузке на сервер включите следующие файлы в zip-архив:

### Обязательные файлы:
- `main.py` - основной файл приложения
- `sim_card_processor.py` - обработчик SIM-карт
- `google_sheets_processor.py` - обработчик Google Sheets
- `requirements.txt` - зависимости Python
- `llms_bot_pachka/` - папка с ботом
- `processing_steps/` - папка с шагами обработки

### Скрипты развертывания:
- `universal_deploy.sh` - универсальный скрипт
- `check_server.sh` - проверка компонентов
- `install_docker.sh` - установка Docker
- `deploy.sh` - развертывание через Docker
- `setup_env.sh` - настройка виртуального окружения
- `update_code.sh` - обновление кода

### Файлы для Docker:
- `Dockerfile`
- `docker-compose.yml`
- `.dockerignore`

### Конфигурационные файлы:
- `client_secret.example.json` (переименуйте в `client_secret.json`)

## 🔧 Настройка переменных окружения

Создайте файл `.env` в корне проекта:
```env
GOOGLE_SHEETS_CREDENTIALS_FILE=client_secret.json
# Добавьте другие переменные по необходимости
```

## 📊 Мониторинг

### Docker:
```bash
# Просмотр логов
docker-compose logs -f  # или docker compose logs -f

# Статус контейнера
docker-compose ps  # или docker compose ps

# Остановка
docker-compose down  # или docker compose down
```

### Виртуальное окружение:
```bash
# Проверка процессов
ps aux | grep python

# Остановка процесса
pkill -f "python main.py"
```

## 🚨 Устранение неполадок

### Проблема: Не устанавливаются зависимости
```bash
# Обновите pip
pip install --upgrade pip

# Установите системные зависимости (для Ubuntu/Debian)
sudo apt install python3-dev build-essential
```

### Проблема: Не хватает места на диске
```bash
# Очистка Docker
docker system prune -a

# Очистка pip кэша
pip cache purge
```

### Проблема: Порт занят
```bash
# Найдите процесс, использующий порт
sudo netstat -tulpn | grep :8000

# Остановите процесс
sudo kill -9 <PID>
```

## 📝 Автоматизация

Для автоматического обновления при загрузке нового zip-архива:

1. Создайте папку для загрузок: `mkdir uploads`
2. Настройте автоматический скрипт:
```bash
#!/bin/bash
# auto_update.sh
while true; do
    if [ -f "uploads/new_version.zip" ]; then
        unzip -o uploads/new_version.zip -d .
        rm uploads/new_version.zip
        ./universal_deploy.sh
    fi
    sleep 30
done
```

## 🎯 Рекомендуемый workflow

1. **Первый раз на сервере:**
   ```bash
   ./check_server.sh
   ./universal_deploy.sh
   ```

2. **При обновлении кода:**
   ```bash
   # Загрузите новый zip-архив
   # Распакуйте его
   ./universal_deploy.sh
   ```

3. **Для мониторинга:**
   ```bash
   # Docker
   docker-compose logs -f
   
   # Виртуальное окружение
   ps aux | grep python
   ``` 