# 🔒 Безопасность

## Важные файлы с секретами

Следующие файлы содержат чувствительные данные и **НЕ ДОЛЖНЫ** попадать в репозиторий:

- `.env` - переменные окружения с токенами и ключами
- `bots_config.json` - конфигурация ботов с токенами доступа
- `client_secret.json` - учетные данные Google API
- `token.json` - токен авторизации Google OAuth
- `*.service` - файлы systemd могут содержать секреты

Все эти файлы добавлены в `.gitignore` и не будут загружены в репозиторий.

## Переменные окружения

Все секреты должны храниться в переменных окружения. Используйте файл `.env` для локальной разработки.

### Обязательные переменные:

- `PACHKA_TOKEN` - токен webhook для Pachka
- `GOOGLE_SHEETS_ID` - ID таблицы Google Sheets

### Опциональные переменные:

- `PACHKA_API_TOKEN` - API токен для отправки в конкретные чаты
- `PACHKA_WEBHOOK_URL` - URL webhook (если отличается от значения по умолчанию)
- `SERVER_HOST` - хост для Flask сервера (по умолчанию 0.0.0.0)
- `SERVER_PORT` - порт для Flask сервера (по умолчанию 5000)

## Настройка

1. Скопируйте примеры файлов:
   ```bash
   cp env.example .env
   cp bots_config.example.json bots_config.json
   cp client_secret.example.json client_secret.json
   ```

2. Заполните `.env` реальными значениями

3. Заполните `bots_config.json` реальными значениями (если используете universal_bot.py)

4. Настройте `client_secret.json` с учетными данными из Google Cloud Console

## Проверка безопасности

Перед коммитом убедитесь, что:
- ✅ Файл `.env` не добавлен в git
- ✅ Файл `bots_config.json` не добавлен в git (используйте `bots_config.example.json`)
- ✅ Файл `client_secret.json` не добавлен в git
- ✅ Нет хардкодных токенов в коде

