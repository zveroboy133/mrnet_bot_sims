# Переменные окружения для бота Pachka
# Скопируйте этот файл в .env и заполните реальными значениями

# Токен для работы с API Pachka (webhook токен)
# Получите токен в настройках бота в Pachka: Автоматизации -> Webhooks
PACHKA_TOKEN=ваш_webhook_токен_здесь

# API токен для отправки сообщений в конкретные чаты (опционально)
# Получите токен в настройках бота в Pachka: Автоматизации -> API
PACHKA_API_TOKEN=ваш_api_токен_здесь

# Webhook URL для отправки сообщений (опционально, если не указан, берется из bots_config.json)
PACHKA_WEBHOOK_URL=https://api.pachca.com/webhooks/ваш_webhook_id

# ID таблицы Google Sheets
GOOGLE_SHEETS_ID=ваш_spreadsheet_id_здесь

# IP адрес или хост сервера для Flask (по умолчанию 0.0.0.0)
SERVER_HOST=0.0.0.0

# Порт для Flask сервера (по умолчанию 5000)
SERVER_PORT=5000

# Дополнительные настройки (опционально)
# DEBUG=true
# LOG_LEVEL=INFO 