# 📋 Инструкция по проекту Find SIMs

## 📖 Описание проекта

Find SIMs - это система для управления и мониторинга SIM-карт с интеграцией в Google Sheets и ботом для мессенджера Pachka. Проект позволяет:

- 📱 Отслеживать состояние SIM-карт
- 🔍 Искать информацию по номерам телефонов и устройствам
- 🤖 Получать уведомления через бота в Pachka
- 📊 Работать с данными через Google Sheets
- 📸 Обрабатывать изображения SIM-карт с помощью OCR

---

## 🚀 Часть 1: Установка и настройка репозитория

### 1.1 Подготовка системы

#### Для Windows:
```bash
# Установите Python 3.8+ с официального сайта
# Установите Git для Windows
# Откройте PowerShell или Command Prompt
```

#### Для Linux/Ubuntu:
```bash
# Обновите систему
sudo apt update && sudo apt upgrade -y

# Установите необходимые пакеты
sudo apt install python3 python3-pip python3-venv git curl wget -y
```

### 1.2 Клонирование репозитория

```bash
# Клонируйте репозиторий
git clone <URL_репозитория>
cd find_sims-main

# Или скачайте и распакуйте архив
# Перейдите в папку проекта
```

### 1.3 Настройка виртуального окружения

#### Автоматическая настройка (рекомендуется):
```bash
# Запустите скрипт настройки окружения
./scripts/setup_env.sh

# Активируйте виртуальное окружение
source find_sims_env/bin/activate  # Linux/Mac
# или
find_sims_env\Scripts\activate     # Windows
```

#### Ручная настройка:
```bash
# Создайте виртуальное окружение
python3 -m venv find_sims_env

# Активируйте окружение
source find_sims_env/bin/activate  # Linux/Mac
# или
find_sims_env\Scripts\activate     # Windows

# Установите зависимости
pip install -r requirements.txt
```

### 1.4 Настройка Google Sheets API

#### 1.4.1 Создание проекта в Google Cloud Console

1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите API:
   - Google Sheets API
   - Google Drive API

#### 1.4.2 Создание учетных данных

1. Перейдите в "APIs & Services" → "Credentials"
2. Нажмите "Create Credentials" → "OAuth 2.0 Client IDs"
3. Выберите тип приложения: "Desktop application"
4. Скачайте JSON-файл с учетными данными

#### 1.4.3 Настройка файла учетных данных

```bash
# Скопируйте пример файла
cp client_secret.example.json client_secret.json

# Замените содержимое client_secret.json на скачанные данные
# При первом запуске откроется браузер для авторизации
```

### 1.5 Настройка переменных окружения

#### 1.5.1 Создание файла .env

```bash
# Скопируйте пример файла
cp env.example .env

# Отредактируйте файл .env
nano .env  # или используйте любой текстовый редактор
```

#### 1.5.2 Содержимое файла .env

```env
# Токен для работы с API Pachka
# Получите токен в настройках бота в Pachka: Автоматизации -> API
PACHKA_TOKEN=ваш_токен_здесь

# Дополнительные настройки (опционально)
DEBUG=true
LOG_LEVEL=INFO
```

### 1.6 Получение токена Pachka

#### 1.6.1 Настройка бота в Pachka

1. Откройте Pachka
2. Перейдите в настройки вашего бота
3. Выберите "Автоматизации" → "API"
4. Создайте новый API токен
5. Скопируйте токен (должен начинаться с `pachca_`)

#### 1.6.2 Обновление .env файла

```bash
# Замените "ваш_токен_здесь" на реальный токен
PACHKA_TOKEN=pachca_ваш_реальный_токен
```

### 1.7 Проверка установки

```bash
# Проверьте, что все компоненты установлены
./scripts/check_server.sh

# Протестируйте Google Sheets подключение
python3 -c "from google_sheets_processor import GoogleSheetsProcessor; print('Google Sheets подключение работает')"

# Проверьте токен Pachka
python3 -c "import os; from dotenv import load_dotenv; load_dotenv(); print('Токен Pachka:', os.getenv('PACHKA_TOKEN')[:10] + '...')"
```

---

## 🤖 Часть 2: Запуск бота

### 2.1 Запуск в режиме разработки

#### Простой запуск:
```bash
# Активируйте виртуальное окружение (если не активировано)
source find_sims_env/bin/activate  # Linux/Mac
# или
find_sims_env\Scripts\activate     # Windows

# Запустите бота
python3 start_bot.py
```

#### Альтернативный запуск:
```bash
# Прямой запуск бота
python3 llms_bot_pachka/bot.py
```

### 2.2 Запуск через скрипты

#### Использование готовых скриптов:
```bash
# Запуск бота через скрипт
./scripts/run_bot.sh

# Запуск бота как демон
./scripts/run_bot_daemon.sh

# Настройка бота как системный сервис
./scripts/setup_bot_service.sh
```

### 2.3 Запуск через Docker (рекомендуется для продакшена)

#### Первоначальная настройка Docker:
```bash
# Установите Docker (если не установлен)
./scripts/install_docker.sh

# Запустите через Docker Compose
docker-compose up -d

# Или используйте новый формат
docker compose up -d
```

#### Управление Docker контейнером:
```bash
# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Перезапуск
docker-compose restart
```

### 2.4 Проверка работоспособности

#### Проверка статуса бота:
```bash
# Проверьте, что бот запущен
./scripts/check_bot_status.sh

# Проверьте процессы
ps aux | grep python

# Проверьте логи
tail -f bot.log
```

#### Тестирование в Pachka:
1. Откройте чат с ботом в Pachka
2. Отправьте команду `/start`
3. Бот должен ответить приветственным сообщением

---

## 💬 Часть 3: Работа с ботом после старта

### 3.1 Основные команды бота

#### Команды для поиска:
```
/start - Запуск бота и приветствие
/help - Справка по командам
/search_phone <номер> - Поиск по номеру телефона
/search_device <название> - Поиск по названию устройства
/check_activity <устройство> - Проверка активности SIM-карт устройства
```

#### Примеры использования:
```
/search_phone +79001234567
/search_device donuzlav-aquaculture-001
/check_activity router-001
```

### 3.2 Функции бота

#### 3.2.1 Поиск по номеру телефона
- Бот ищет SIM-карту по номеру телефона в Google Sheets
- Возвращает полную информацию о найденной записи
- Показывает статус, оператора, тариф и другие данные

#### 3.2.2 Поиск по устройству
- Поиск всех SIM-карт, привязанных к конкретному устройству
- Отображает таблицу с результатами
- Показывает состояние каждой SIM-карты

#### 3.2.3 Проверка активности
- Анализирует активность SIM-карт для указанного устройства
- Отправляет подробный отчет
- Уведомляет о проблемах с SIM-картами

### 3.3 Работа с Google Sheets

#### 3.3.1 Структура данных
Бот работает с Google Sheets таблицей, содержащей следующие колонки:
- Оператор
- Дата прихода SIM
- Мобильный номер (MSISDN)
- ICCID
- Дата активации
- Тип SIM-карты
- Тип модемов
- Адрес установки
- Состояние SIM-карт
- Устройство
- И другие...

#### 3.3.2 Автоматическое обновление
- Бот автоматически синхронизируется с Google Sheets
- Изменения в таблице отражаются в реальном времени
- Поддерживается авторизация через OAuth 2.0

### 3.4 Мониторинг и логирование

#### 3.4.1 Просмотр логов:
```bash
# Просмотр логов в реальном времени
tail -f bot.log

# Поиск ошибок
grep "ERROR" bot.log

# Поиск конкретных событий
grep "search_phone" bot.log
```

#### 3.4.2 Мониторинг через скрипты:
```bash
# Проверка статуса бота
./scripts/check_bot_status.sh

# Проверка сервера
./scripts/check_server.sh
```

### 3.5 Устранение неполадок

#### 3.5.1 Проблемы с токеном:
```bash
# Проверьте токен в .env файле
cat .env | grep PACHKA_TOKEN

# Пересоздайте токен в Pachka и обновите .env
./scripts/setup_pachka_token.sh
```

#### 3.5.2 Проблемы с Google Sheets:
```bash
# Проверьте файл client_secret.json
ls -la client_secret.json

# Пересоздайте учетные данные в Google Cloud Console
# Удалите token.json для повторной авторизации
rm token.json
```

#### 3.5.3 Проблемы с подключением:
```bash
# Проверьте интернет-соединение
ping google.com

# Проверьте доступность API Pachka
curl -I https://api.pachca.com/api
```

### 3.6 Обновление и обслуживание

#### 3.6.1 Обновление кода:
```bash
# Автоматическое обновление
./scripts/update_code.sh

# Ручное обновление
git pull origin main
pip install -r requirements.txt
```

#### 3.6.2 Перезапуск бота:
```bash
# Остановка бота
pkill -f "python.*bot.py"

# Запуск бота
python3 start_bot.py

# Или через Docker
docker-compose restart
```

#### 3.6.3 Резервное копирование:
```bash
# Создание резервной копии конфигурации
cp .env .env.backup
cp client_secret.json client_secret.json.backup

# Восстановление из резервной копии
cp .env.backup .env
cp client_secret.json.backup client_secret.json
```

---

## 📚 Дополнительные материалы

### Полезные файлы проекта:
- `README.md` - Основная документация
- `DEPLOYMENT.md` - Инструкция по развертыванию
- `TOKEN_SETUP.md` - Настройка токенов
- `AUTO_DEPLOY_SETUP.md` - Автоматическое развертывание

### Скрипты автоматизации:
- `scripts/universal_deploy.sh` - Универсальный скрипт развертывания
- `scripts/setup_env.sh` - Настройка окружения
- `scripts/run_bot.sh` - Запуск бота
- `scripts/check_bot_status.sh` - Проверка статуса

### Контакты и поддержка:
- При возникновении проблем проверьте логи: `tail -f bot.log`
- Убедитесь, что все зависимости установлены: `pip list`
- Проверьте настройки в файлах `.env` и `client_secret.json`

---

## ✅ Чек-лист для проверки установки

- [ ] Python 3.8+ установлен
- [ ] Виртуальное окружение создано и активировано
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Файл `client_secret.json` настроен
- [ ] Файл `.env` создан с токеном Pachka
- [ ] Google Sheets API настроен
- [ ] Бот запускается без ошибок
- [ ] Команда `/start` работает в Pachka
- [ ] Поиск по номеру телефона работает
- [ ] Поиск по устройству работает
- [ ] Логи записываются в `bot.log`

---

*Инструкция создана для проекта Find SIMs. Последнее обновление: 2024* 