# MRNet Bot SIMs

Бот для поиска информации о SIM-картах в Google Sheets через мессенджер Pachka.

## Возможности

- 🔍 Поиск SIM-карт по номеру телефона
- 📱 Поиск SIM-карт по устройству
- 📊 Проверка активности SIM-карт для устройства
- 🤖 Работа через бота в Pachka

## Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ваш_username/mrnet_bot_sims.git
cd mrnet_bot_sims
```

2. Создайте виртуальное окружение:
```bash
python3 -m venv find_sims_env
source find_sims_env/bin/activate  # Linux/Mac
# или
find_sims_env\Scripts\activate     # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Настройте переменные окружения:
```bash
cp env.example .env
cp bots_config.example.json bots_config.json
cp client_secret.example.json client_secret.json
```

5. Заполните файл `.env` реальными значениями (см. `env.example`)

6. Настройте Google Sheets API:
   - Создайте проект в [Google Cloud Console](https://console.cloud.google.com/)
   - Включите Google Sheets API и Google Drive API
   - Создайте учетные данные OAuth 2.0 (Desktop app)
   - Замените содержимое `client_secret.json` на скачанные данные

## Проверка конфигурации

Перед запуском проверьте конфигурацию:

```bash
python check_config.py
```

Или используйте встроенную проверку:
```bash
python start_bot.py --check
python start_bot2.py --check
```

## Запуск

### Основной бот:
```bash
python start_bot.py
```

### Второй бот (универсальный):
```bash
python start_bot2.py          # Запуск bot2
python start_bot2.py bot1     # Запуск bot1 из конфига
python start_bot2.py bot2     # Запуск bot2 из конфига
```

### Справка:
```bash
python start_bot.py --help    # Справка по основному боту
python start_bot2.py --help   # Справка по универсальному боту
```

### Через скрипты:
```bash
./scripts/run_bot.sh          # Обычный запуск
./scripts/run_bot_daemon.sh    # Запуск как демон
```

## Переменные окружения

Все секреты хранятся в файле `.env`. См. `env.example` для списка переменных.

**Обязательные:**
- `PACHKA_TOKEN` - токен webhook для Pachka
- `GOOGLE_SHEETS_ID` - ID таблицы Google Sheets

**Опциональные:**
- `PACHKA_API_TOKEN` - API токен для отправки в конкретные чаты
- `PACHKA_WEBHOOK_URL` - URL webhook
- `SERVER_HOST` - хост для Flask сервера (по умолчанию 0.0.0.0)
- `SERVER_PORT` - порт для Flask сервера (по умолчанию 5000)

## Безопасность

⚠️ **ВАЖНО:** Файлы с секретами (`.env`, `bots_config.json`, `client_secret.json`) не должны попадать в репозиторий!

См. [SECURITY.md](SECURITY.md) для подробной информации.

## Использование

После запуска бота отправьте команды в чат с ботом:

- `/start` - приветствие и список команд
- `/active [устройство]` - проверка активности SIM-карт для устройства
- `/new [текст]` - отправка текста через webhook

## Документация

- [ИНСТРУКЦИЯ_ПО_ПРОЕКТУ.md](ИНСТРУКЦИЯ_ПО_ПРОЕКТУ.md) - подробная инструкция
- [SECURITY.md](SECURITY.md) - информация о безопасности