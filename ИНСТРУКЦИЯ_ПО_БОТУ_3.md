# 🤖 Инструкция по настройке третьего бота

## 📋 Пошаговая инструкция

### Шаг 0: Обновление репозитория и проверка структуры

**ВАЖНО:** Перед началом настройки убедитесь, что у вас последняя версия кода из репозитория!

1. **Проверьте структуру папок на сервере:**
   
   Если у вас несколько папок с ботами (например, `find_sims` для bot1/bot2 и `mrnet_bot_sims` для bot3):
   ```bash
   ls -la ~/
   # Должны увидеть папки, например:
   # find_sims/          - для bot1 и bot2
   # mrnet_bot_sims/     - для bot3
   ```

2. **Проверьте виртуальное окружение:**
   ```bash
   # Проверьте, где находится виртуальное окружение
   ls -la ~/find_sims/find_sims_env 2>/dev/null
   ls -la ~/mrnet_bot_sims/find_sims_env 2>/dev/null
   ```
   
   **Если виртуальное окружение есть только в `find_sims`:**
   - Создайте отдельное виртуальное окружение для bot3 (см. ниже)
   - Или используйте существующее (если зависимости одинаковые)
   
   **Если виртуального окружения нет в `mrnet_bot_sims`:**
   ```bash
   cd ~/mrnet_bot_sims
   python3 -m venv find_sims_env
   ```

3. **Перейдите в папку проекта для bot3:**
   ```bash
   cd ~/mrnet_bot_sims
   # или
   cd /path/to/your/project
   ```

2. **Проверьте текущий статус:**
   ```bash
   git status
   ```

3. **Получите последние изменения из GitHub:**
   ```bash
   git pull origin main
   ```
   
   Если репозиторий еще не клонирован, выполните:
   ```bash
   git clone https://github.com/zveroboy133/mrnet_bot_sims.git
   cd mrnet_bot_sims
   ```

4. **Проверьте, что файлы обновились:**
   ```bash
   ls -la start_bot3.py
   ls -la bots_config.example.json
   ```

5. **Если были изменения в зависимостях, обновите их:**
   ```bash
   source find_sims_env/bin/activate
   pip install -r requirements.txt
   ```

**Примечание:** Если вы работаете с существующим проектом, где уже настроены bot1 и bot2, убедитесь, что вы обновляете правильный репозиторий. Каждый бот может использовать свой репозиторий или все боты могут быть в одном репозитории.

### Шаг 1: Настройка конфигурации бота

1. **Скопируйте файл конфигурации** (если еще не скопирован):
   ```bash
   cp bots_config.example.json bots_config.json
   ```

2. **Откройте файл `bots_config.json`** и найдите секцию `bot3` (или добавьте её, если её нет).

3. **Заполните конфигурацию для bot3:**
   ```json
   "bot3": {
     "name": "Третий бот",
     "port": 5002,
     "webhook_incoming": "https://api.pachca.com/webhooks/ВАШ_WEBHOOK_ID_3",
     "webhook_outgoing": "http://91.217.77.71:5002/webhook",
     "signing_secret": "ваш_signing_secret_здесь",
     "user_id": 0,
     "access_token": "ваш_access_token_здесь",
     "service_name": "pachka-bot-3"
   }
   ```

### Шаг 2: Настройка webhook в Pachka

1. **Откройте Pachka** и перейдите в настройки вашего третьего бота.

2. **Создайте webhook:**
   - Перейдите в **Автоматизации** → **Webhooks**
   - Создайте новый webhook
   - Скопируйте **Webhook URL** (он будет выглядеть как `https://api.pachca.com/webhooks/XXXXX`)
   - Вставьте этот URL в поле `webhook_incoming` в `bots_config.json`

3. **Создайте API токен:**
   - Перейдите в **Автоматизации** → **API**
   - Создайте новый API токен
   - Скопируйте токен (должен начинаться с `pachca_`)
   - Вставьте этот токен в поле `access_token` в `bots_config.json`

4. **Настройте входящий webhook:**
   - В настройках webhook укажите URL вашего сервера: `http://91.217.77.71:5002/webhook`
   - Убедитесь, что порт 5002 открыт в файрволе
   
   **Примечание:** Для сервера `91.217.77.71` используйте указанный выше URL. Если у вас другой сервер, замените IP адрес на свой.

### Шаг 3: Настройка переменных окружения

1. **Убедитесь, что файл `.env` существует:**
   ```bash
   cp env.example .env
   ```

2. **Откройте файл `.env`** и проверьте, что там есть хотя бы:
   ```env
   GOOGLE_SHEETS_ID=ваш_spreadsheet_id_здесь
   ```
   
   **Примечание:** Для работы скрипта экспорта ICCID:IMEI требуется Google Sheets ID.

### Шаг 3.1: Настройка Google Sheets API

Для работы скрипта экспорта требуется настройка Google Sheets API:

1. **Получите файл `credentials.json`** (или `client_secret.json`):
   - Создайте проект в Google Cloud Console
   - Включите Google Sheets API и Google Drive API
   - Создайте OAuth 2.0 Client ID (тип: Desktop application)
   - Скачайте JSON файл с учётными данными

2. **Поместите файл в проект:**
   ```bash
   # Рекомендуемое расположение (в корне проекта)
   cp ~/Downloads/client_secret.json ~/mrnet_bot_sims/credentials.json
   
   # Или в папку скрипта
   cp ~/Downloads/client_secret.json ~/mrnet_bot_sims/iccid_imei_export/credentials.json
   ```

3. **Создайте `token.json`** (один раз):
   
   **Вариант А: На сервере с доступом к браузеру (SSH с X11 forwarding):**
   ```bash
   cd ~/mrnet_bot_sims/iccid_imei_export
   source ~/find_sims/find_sims_env/bin/activate
   python3 main.py
   # Откроется браузер для авторизации, после чего создастся token.json
   ```
   
   **Вариант Б: На локальном компьютере, затем скопировать на сервер:**
   - Запустите скрипт локально для авторизации
   - Скопируйте созданный `token.json` на сервер:
     ```bash
     scp token.json titkov@91.217.77.71:~/mrnet_bot_sims/token.json
     ```
   
   **Вариант В: Использовать существующий `token.json` из другого проекта:**
   ```bash
   # Если token.json уже есть где-то на сервере
   find ~ -name "token.json" -type f
   # Скопируйте найденный файл
   cp /путь/к/token.json ~/mrnet_bot_sims/token.json
   ```

4. **Проверьте расположение файлов:**
   ```bash
   # credentials.json должен быть в одном из мест:
   ls -la ~/mrnet_bot_sims/credentials.json
   ls -la ~/mrnet_bot_sims/iccid_imei_export/credentials.json
   
   # token.json должен быть в той же папке, что и credentials.json
   ls -la ~/mrnet_bot_sims/token.json
   ```

### Шаг 4: Проверка и исправление JSON конфигурации

**ВАЖНО:** Перед проверкой конфигурации убедитесь, что файл `bots_config.json` имеет правильный JSON синтаксис!

1. **Проверьте синтаксис JSON файла:**
   ```bash
   # Активируйте виртуальное окружение
   source find_sims_env/bin/activate
   
   # Проверьте JSON синтаксис
   python3 -c "import json; json.load(open('bots_config.json', 'r', encoding='utf-8')); print('JSON валиден')"
   ```
   
   **Альтернативный вариант (если нужен восклицательный знак):**
   ```bash
   python3 -c 'import json; json.load(open("bots_config.json", "r", encoding="utf-8")); print("JSON валиден!")'
   ```
   
   Если есть ошибка, вы увидите сообщение с указанием строки и позиции ошибки.

2. **Частые ошибки в JSON:**
   - Отсутствие запятой между объектами (после `}` должна быть запятая, кроме последнего)
   - Лишние запятые (после последнего элемента в объекте/массиве)
   - Кавычки должны быть двойными `"`, не одинарными `'`
   - Все строки должны быть в кавычках

3. **Пример правильной структуры:**
   ```json
   {
     "bots": {
       "bot1": {
         "name": "Основной бот",
         "port": 5000,
         ...
       },
       "bot2": {
         "name": "Второй бот",
         "port": 5001,
         ...
       },
       "bot3": {
         "name": "Третий бот",
         "port": 5002,
         ...
       }
     }
   }
   ```
   
   **Обратите внимание:** После `bot2` и перед `bot3` должна быть запятая!

4. **Если файл поврежден, скопируйте заново:**
   ```bash
   cp bots_config.example.json bots_config.json
   # Затем отредактируйте bots_config.json и заполните своими данными
   ```

### Шаг 5: Проверка конфигурации

**ВАЖНО:** Перед проверкой конфигурации обязательно активируйте виртуальное окружение!

1. **Проверьте, существует ли виртуальное окружение:**
   ```bash
   ls -la find_sims_env/bin/activate
   ```
   
   Если файл не найден, создайте виртуальное окружение:
   ```bash
   python3 -m venv find_sims_env
   ```

2. **Активируйте виртуальное окружение:**
   ```bash
   source find_sims_env/bin/activate
   ```
   
   После активации в начале строки терминала должно появиться `(find_sims_env)`.

3. **Установите зависимости (если еще не установлены):**
   ```bash
   pip install -r requirements.txt
   ```

4. **Проверьте конфигурацию:**
   ```bash
   python3 start_bot3.py --check
   ```
   
   Или используйте общую проверку:
   ```bash
   python3 check_config.py
   ```
   
   **Примечание:** Если вы забыли активировать виртуальное окружение, получите ошибку `ModuleNotFoundError`. Всегда активируйте окружение перед запуском команд!

### Шаг 6: Локальный запуск (для тестирования)

1. **Активируйте виртуальное окружение:**
   ```bash
   # Linux/Mac
   source find_sims_env/bin/activate
   
   # Windows
   find_sims_env\Scripts\activate
   ```

2. **Запустите бота:**
   ```bash
   python3 start_bot3.py
   ```
   
   Или используйте Python из виртуального окружения:
   ```bash
   find_sims_env/bin/python start_bot3.py
   ```

3. **Проверьте работу:**
   - Бот должен вывести сообщение о запуске
   - Откройте чат с ботом в Pachka
   - Отправьте команду `/start` - бот должен ответить приветственным сообщением
   - Отправьте команду `/run_script` - бот должен запустить скрипт экспорта и отправить ссылки на файлы

### Шаг 7: Настройка для удаленного сервера

#### 6.1 Настройка systemd сервиса

1. **ВАЖНО: Проверьте виртуальное окружение:**
   
   **Если у вас две папки с ботами (`find_sims` и `mrnet_bot_sims`):**
   
   ```bash
   # Проверьте, где находится виртуальное окружение
   ls -la ~/find_sims/find_sims_env
   ls -la ~/mrnet_bot_sims/find_sims_env
   ```
   
   **Если виртуальное окружение есть только в `find_sims`:**
   
   **📚 Как это работает:**
   
   Виртуальное окружение — это просто папка с Python и установленными библиотеками. Его можно использовать из любого места, указав полный путь к Python из этого окружения.
   
   **Плюсы использования общего окружения:**
   - Экономия места на диске
   - Одинаковые версии библиотек для всех ботов
   - Проще обновлять зависимости (один раз для всех)
   
   **Минусы:**
   - Если боты требуют разные версии библиотек — конфликт
   - Если один бот сломает окружение — пострадают все
   
   **Вариант А: Создайте отдельное виртуальное окружение для bot3 (рекомендуется):**
   ```bash
   cd ~/mrnet_bot_sims
   python3 -m venv find_sims_env
   source find_sims_env/bin/activate
   pip install -r requirements.txt
   ```
   
   **Вариант Б: Используйте виртуальное окружение из `find_sims` (если зависимости одинаковые):**
   
   Это возможно! Просто укажите правильный путь в systemd сервисе:
   ```bash
   # В systemd сервисе используйте путь к окружению из find_sims
   # ExecStart=/home/titkov/find_sims/find_sims_env/bin/python /home/titkov/mrnet_bot_sims/start_bot3.py
   ```
   
   **Как это работает:**
   - Python из `/home/titkov/find_sims/find_sims_env/bin/python` знает, где искать библиотеки (в своем окружении)
   - Скрипт `start_bot3.py` запускается из `/home/titkov/mrnet_bot_sims/`
   - Python находит библиотеки в `/home/titkov/find_sims/find_sims_env/lib/`
   - Всё работает, потому что Python ищет библиотеки относительно своего расположения, а не относительно скрипта
   
   **Пример правильной конфигурации systemd для общего окружения:**
   ```ini
   [Service]
   WorkingDirectory=/home/titkov/mrnet_bot_sims
   ExecStart=/home/titkov/find_sims/find_sims_env/bin/python /home/titkov/mrnet_bot_sims/start_bot3.py
   ```
   
   **Важно:** `WorkingDirectory` должен указывать на папку с bot3, а `ExecStart` использует Python из другого окружения.
   
   **Если виртуальное окружение есть в `mrnet_bot_sims`:**
   ```bash
   cd ~/mrnet_bot_sims
   source find_sims_env/bin/activate
   # Проверьте, что это правильное окружение
   which python
   # Должно быть: /home/titkov/mrnet_bot_sims/find_sims_env/bin/python
   ```

2. **Установите зависимости в правильное виртуальное окружение:**
   ```bash
   cd ~/mrnet_bot_sims
   
   # Если виртуальное окружение в этой папке
   if [ -d "find_sims_env" ]; then
     source find_sims_env/bin/activate
     /home/titkov/mrnet_bot_sims/find_sims_env/bin/python -m pip install -r requirements.txt
   else
     # Создайте виртуальное окружение
     python3 -m venv find_sims_env
     source find_sims_env/bin/activate
     pip install -r requirements.txt
   fi
   
   # Установите зависимости для скрипта экспорта (если используется общее окружение)
   # Если используется окружение из find_sims, установите зависимости там:
   source ~/find_sims/find_sims_env/bin/activate
   pip install -r iccid_imei_export/requirements.txt
   
   # Проверьте установку
   find_sims_env/bin/python -c "import dotenv; print('OK')"
   ~/find_sims/find_sims_env/bin/python -c "import gspread; print('Google Sheets OK')"
   ```

2. **Создайте файл systemd сервиса:**
   
   **Вариант А: Если файл `pachka-bot-3.service` уже есть в проекте:**
   ```bash
   # Отредактируйте файл, заменив пути
   nano pachka-bot-3.service
   # или
   vi pachka-bot-3.service
   ```
   
   Замените `/path/to/your/project` на реальный путь (например, `/home/titkov/mrnet_bot_sims`)
   
   **Вариант Б: Создайте файл вручную:**
   ```bash
   sudo nano /etc/systemd/system/pachka-bot-3.service
   ```
   
   Вставьте следующее содержимое (замените пути на реальные):
   ```ini
   [Unit]
   Description=Pachka Bot 3 Service
   After=network.target
   
   [Service]
   Type=simple
   User=titkov
   WorkingDirectory=/home/titkov/mrnet_bot_sims
   Environment="PATH=/home/titkov/mrnet_bot_sims/find_sims_env/bin:/usr/local/bin:/usr/bin:/bin"
   ExecStart=/home/titkov/mrnet_bot_sims/find_sims_env/bin/python /home/titkov/mrnet_bot_sims/start_bot3.py
   Restart=always
   RestartSec=10
   StandardOutput=journal
   StandardError=journal
   
   [Install]
   WantedBy=multi-user.target
   ```
   
   **Важно:** 
   - Замените `/home/titkov/mrnet_bot_sims` на реальный путь к вашему проекту
   - Замените `titkov` на ваше имя пользователя
   - Убедитесь, что путь к Python правильный: `/home/titkov/mrnet_bot_sims/find_sims_env/bin/python`

3. **Если файл был создан в проекте, скопируйте его:**
   ```bash
   sudo cp pachka-bot-3.service /etc/systemd/system/
   ```

4. **Перезагрузите systemd:**
   ```bash
   sudo systemctl daemon-reload
   ```

5. **Включите автозапуск:**
   ```bash
   sudo systemctl enable pachka-bot-3.service
   ```

6. **Запустите сервис:**
   ```bash
   sudo systemctl start pachka-bot-3.service
   ```

7. **Проверьте статус:**
   ```bash
   sudo systemctl status pachka-bot-3.service
   ```
   
   **Если видите ошибку "ModuleNotFoundError: No module named 'dotenv'":**
   - Убедитесь, что зависимости установлены (см. шаг 1 выше)
   - Проверьте, что путь к Python правильный в сервисе
   - Попробуйте запустить бот вручную для проверки:
     ```bash
     source find_sims_env/bin/activate
     python3 start_bot3.py
     ```

#### 6.2 Просмотр логов

```bash
# Просмотр логов в реальном времени
sudo journalctl -u pachka-bot-3.service -f

# Просмотр последних 100 строк
sudo journalctl -u pachka-bot-3.service -n 100

# Просмотр логов за определённый период
sudo journalctl -u pachka-bot-3.service --since "17:00" --until "17:05"
```

**Проверка работы планировщика:**
При запуске бота в логах должна быть строка:
```
[Третий бот] Scheduler started: daily task at 17:00 MSK
```

Если этой строки нет, планировщик не запустился - проверьте логи на ошибки.

#### 6.3 Управление сервисом

```bash
# Остановка
sudo systemctl stop pachka-bot-3.service

# Перезапуск
sudo systemctl restart pachka-bot-3.service

# Отключение автозапуска
sudo systemctl disable pachka-bot-3.service
```

#### 6.4 Быстрое обновление и перезапуск

Для обновления кода и перезапуска бота используйте скрипт `restart_bot3.sh`:

```bash
cd ~/mrnet_bot_sims
./restart_bot3.sh
```

Скрипт автоматически:
1. Обновляет репозиторий (`git pull origin main`)
2. Останавливает сервис
3. Перезапускает сервис
4. Проверяет статус

**Перед первым использованием сделайте скрипт исполняемым:**
```bash
chmod +x restart_bot3.sh
```

### Шаг 8: Настройка файрвола

Убедитесь, что порт 5002 открыт:

```bash
# Для ufw (Ubuntu)
sudo ufw allow 5002/tcp

# Для firewalld (CentOS/RHEL)
sudo firewall-cmd --permanent --add-port=5002/tcp
sudo firewall-cmd --reload

# Для iptables
sudo iptables -A INPUT -p tcp --dport 5002 -j ACCEPT
```

### Шаг 9: Проверка работы

1. **Проверьте, что бот запущен:**
   ```bash
   sudo systemctl status pachka-bot-3.service
   ```

2. **Проверьте, что порт слушается:**
   ```bash
   netstat -tlnp | grep 5002
   # или
   ss -tlnp | grep 5002
   ```

3. **Проверьте health endpoint:**
   ```bash
   # Локально на сервере
   curl http://localhost:5002/health
   
   # Извне (с другого компьютера)
   curl http://91.217.77.71:5002/health
   ```
   
   **Если health endpoint не отвечает или возвращает пустой ответ:**
   
   **Проверка 1: Запущен ли бот?**
   ```bash
   # Если бот запущен как сервис
   sudo systemctl status pachka-bot-3.service
   
   # Если бот запущен вручную, проверьте процессы
   ps aux | grep start_bot3
   ps aux | grep python | grep 5002
   ```
   
   **Проверка 2: Слушается ли порт?**
   ```bash
   # Проверьте, что порт 5002 слушается
   netstat -tlnp | grep 5002
   # или
   ss -tlnp | grep 5002
   
   # Должно быть что-то вроде:
   # tcp  0  0  0.0.0.0:5002  0.0.0.0:*  LISTEN  PID/python
   ```
   
   **Проверка 3: Блокирует ли файрвол?**
   ```bash
   # Для ufw
   sudo ufw status | grep 5002
   
   # Если порт не открыт, откройте его
   sudo ufw allow 5002/tcp
   sudo ufw reload
   ```
   
   **Проверка 4: Проверьте логи бота**
   ```bash
   # Если бот запущен как сервис
   sudo journalctl -u pachka-bot-3.service -n 50
   
   # Если бот запущен вручную
   tail -n 50 universal_bot.log
   ```
   
   **Проверка 5: Запустите бот заново**
   ```bash
   # Если бот запущен как сервис
   sudo systemctl restart pachka-bot-3.service
   sudo systemctl status pachka-bot-3.service
   
   # Если бот запущен вручную
   # Остановите старый процесс и запустите заново
   pkill -f start_bot3
   source find_sims_env/bin/activate
   python3 start_bot3.py
   ```

4. **Протестируйте в Pachka:**
   - Откройте чат с ботом
   - Отправьте `/start` - бот должен ответить приветственным сообщением
   - Отправьте `/run_script` - бот должен запустить скрипт экспорта и отправить ссылки на CSV файлы
   - Проверьте, что ссылки на файлы кликабельны и файлы скачиваются

5. **Проверьте автозапуск при перезагрузке сервера:**
   ```bash
   # Проверьте, включен ли автозапуск
   sudo systemctl is-enabled pachka-bot-3.service
   ```
   
   **Должно вывести:** `enabled` (автозапуск включен)
   
   **Если выведет `disabled` (автозапуск выключен):**
   ```bash
   # Включите автозапуск
   sudo systemctl enable pachka-bot-3.service
   
   # Проверьте снова
   sudo systemctl is-enabled pachka-bot-3.service
   ```
   
   **Как это работает:**
   - Команда `systemctl enable` создает символическую ссылку в `/etc/systemd/system/multi-user.target.wants/`
   - При загрузке системы systemd автоматически запускает все сервисы из этой папки
   - Бот будет запускаться автоматически при каждой перезагрузке сервера
   
   **Проверка автозапуска (без перезагрузки):**
   ```bash
   # Посмотрите, есть ли ссылка для автозапуска
   ls -la /etc/systemd/system/multi-user.target.wants/ | grep pachka-bot-3
   ```
   
   Должна быть символическая ссылка на ваш сервис.

## 🔍 Диагностика и отладка

### Проверка логов бота

Бот пишет логи в файл `universal_bot.log` в папке проекта. Также логи доступны через systemd journal.

#### 1. Просмотр логов из файла (если бот запущен вручную):

```bash
# Просмотр последних 50 строк
tail -n 50 universal_bot.log

# Просмотр логов в реальном времени
tail -f universal_bot.log

# Поиск ошибок
grep -i error universal_bot.log

# Поиск webhook запросов
grep -i webhook universal_bot.log
```

#### 2. Просмотр логов через systemd (если бот запущен как сервис):

```bash
# Просмотр логов в реальном времени
sudo journalctl -u pachka-bot-3.service -f

# Просмотр последних 100 строк
sudo journalctl -u pachka-bot-3.service -n 100

# Поиск ошибок
sudo journalctl -u pachka-bot-3.service | grep -i error

# Просмотр логов за последний час
sudo journalctl -u pachka-bot-3.service --since "1 hour ago"
```

### Проверка получения webhook запросов от Pachka

Когда вы отправляете команду `/start` в Pachka, бот должен получить webhook запрос. Проверьте логи:

```bash
# В реальном времени смотрите логи
tail -f universal_bot.log
# или
sudo journalctl -u pachka-bot-3.service -f
```

**Что искать в логах:**

1. **При отправке `/start` в Pachka должно появиться:**
   ```
   === WEBHOOK ENDPOINT CALLED ===
   Received webhook POST request: {...}
   [Третий бот] Received webhook event: {...}
   [Третий бот] Processing command: 'start' in chat ...
   [Третий бот] Sending welcome message
   ```

2. **Если запросов нет:**
   
   **Проверьте настройки webhook в Pachka:**
   - URL должен быть: `http://91.217.77.71:5002/webhook`
   - Webhook должен быть активен
   
   **Проверьте доступность сервера:**
   ```bash
   # Проверьте, что порт слушается
   netstat -tlnp | grep 5002
   # или
   ss -tlnp | grep 5002
   
   # Проверьте доступность извне
   curl http://91.217.77.71:5002/health
   ```
   
   **Проверьте файрвол:**
   ```bash
   # Для ufw
   sudo ufw status | grep 5002
   
   # Если порт не открыт, откройте его
   sudo ufw allow 5002/tcp
   ```

3. **Если запросы есть, но бот не отвечает:**

   **Проверьте логи на наличие ошибок:**
   ```bash
   # Просмотр ошибок в логах
   grep -i error universal_bot.log
   # или
   sudo journalctl -u pachka-bot-3.service | grep -i error
   ```

   **Проверьте отправку сообщений:**
   ```bash
   # Проверьте, отправляются ли сообщения
   grep "Sending webhook message" universal_bot.log
   # или
   sudo journalctl -u pachka-bot-3.service | grep "Sending webhook message"
   
   # Проверьте статус ответа от webhook
   grep "Webhook response" universal_bot.log
   # или
   sudo journalctl -u pachka-bot-3.service | grep "Webhook response"
   ```

   **Проверьте конфигурацию:**
   ```bash
   # Проверьте access_token в конфигурации
   grep "access_token" bots_config.json
   
   # Проверьте webhook_incoming в конфигурации
   grep "webhook_incoming" bots_config.json
   ```

   **Проверьте доступность webhook для отправки:**
   ```bash
   # Тест отправки через webhook (замените WEBHOOK_ID на реальный из bots_config.json)
   curl -X POST "https://api.pachca.com/webhooks/ВАШ_WEBHOOK_ID" \
     -H "Content-Type: application/json" \
     -d '{"message": "Тестовое сообщение от bot3"}'
   ```

   **Проверьте, что webhook endpoint доступен:**
   ```bash
   # Проверьте health endpoint
   curl http://91.217.77.71:5002/health
   
   # Проверьте webhook endpoint (должен вернуть ошибку метода, но не 404)
   curl -X GET http://91.217.77.71:5002/webhook
   ```

   **Проверьте настройки в Pachka:**
   - Убедитесь, что в Pachka webhook настроен на: `http://91.217.77.71:5002/webhook`
   - Проверьте, что webhook активен в настройках Pachka
   - Убедитесь, что `webhook_incoming` в `bots_config.json` правильный

### Проверка отправки сообщений в Pachka

Бот отправляет сообщения через webhook (`webhook_incoming`). Проверьте в логах:

```bash
# Ищите строки с "Sending webhook message"
grep "Sending webhook message" universal_bot.log
# или
sudo journalctl -u pachka-bot-3.service | grep "Sending webhook message"
```

**Что должно быть в логах при успешной отправке:**
```
[Третий бот] Sending webhook message: Привет! Я Третий бот...
[Третий бот] Webhook response: 200
[Третий бот] Webhook message sent successfully
```

**Если статус не 200:**
- Проверьте `webhook_incoming` в `bots_config.json`
- Убедитесь, что webhook URL правильный
- Проверьте, что webhook активен в Pachka

### Проверка доступности сервера из интернета

```bash
# Проверьте, что порт слушается
netstat -tlnp | grep 5002
# или
ss -tlnp | grep 5002

# Проверьте health endpoint локально
curl http://localhost:5002/health

# Проверьте health endpoint извне (с другого компьютера или через онлайн-сервис)
curl http://91.217.77.71:5002/health
```

### Проверка настроек webhook в Pachka

1. **Проверьте webhook URL:**
   - В Pachka: **Автоматизации** → **Webhooks**
   - Убедитесь, что URL указан правильно: `http://91.217.77.71:5002/webhook`
   - Убедитесь, что webhook активен

2. **Проверьте webhook для отправки сообщений:**
   - В Pachka: **Автоматизации** → **Webhooks**
   - Найдите webhook, который используется для отправки (URL из `webhook_incoming`)
   - Убедитесь, что он активен

3. **Проверьте API токен:**
   - В Pachka: **Автоматизации** → **API**
   - Убедитесь, что токен правильный и активен
   - Токен должен начинаться с `pachca_` (не `pachca_wh_`)

### Тестирование webhook вручную

Можно отправить тестовый запрос на webhook endpoint:

**С сервера (локально):**
```bash
# Тестовый запрос на webhook (имитация запроса от Pachka)
curl -X POST http://localhost:5002/webhook \
  -H "Content-Type: application/json" \
  -d '{
    "type": "message",
    "event": "new",
    "content": "/start",
    "chat_id": "12345"
  }'
```

**Извне (с другого компьютера или для проверки доступности):**
```bash
# Тестовый запрос на webhook с указанием IP сервера
curl -X POST http://91.217.77.71:5002/webhook \
  -H "Content-Type: application/json" \
  -d '{
    "type": "message",
    "event": "new",
    "content": "/start",
    "chat_id": "12345"
  }'
```

Если бот работает, вы должны увидеть в логах обработку этого запроса:
```bash
# Смотрите логи в реальном времени
tail -f universal_bot.log
# или
sudo journalctl -u pachka-bot-3.service -f
```

### Проверка конфигурации

```bash
# Проверьте JSON синтаксис
python3 -c "import json; json.load(open('bots_config.json', 'r', encoding='utf-8')); print('JSON валиден')"

# Проверьте конфигурацию бота
source find_sims_env/bin/activate
python3 start_bot3.py --check
```

## 🔧 Устранение неполадок

### Бот не запускается

1. Проверьте логи:
   ```bash
   sudo journalctl -u pachka-bot-3.service -n 50
   ```

2. Проверьте конфигурацию:
   ```bash
   # С активированным виртуальным окружением
   source find_sims_env/bin/activate
   python3 start_bot3.py --check
   
   # Или без активации
   find_sims_env/bin/python start_bot3.py --check
   ```

3. Убедитесь, что порт 5002 свободен:
   ```bash
   netstat -tlnp | grep 5002
   ```

### Ошибка "Expecting ',' delimiter" или "JSON decode error"

Эта ошибка означает, что в файле `bots_config.json` есть синтаксическая ошибка.

**Решение:**

1. **Проверьте синтаксис JSON:**
   ```bash
   source find_sims_env/bin/activate
   python3 -c "import json; json.load(open('bots_config.json', 'r', encoding='utf-8')); print('JSON валиден')"
   ```

2. **Частые проблемы:**
   - Отсутствие запятой после `bot2` (должно быть `},` перед `"bot3"`)
   - Лишняя запятая после последнего элемента в объекте
   - Неправильные кавычки (используйте двойные `"`, не одинарные `'`)

3. **Исправьте файл (конкретно для ошибки на строке 23):**
   
   **Вариант А: Использование nano (проще для новичков):**
   ```bash
   nano bots_config.json
   ```
   
   - Найдите строку 22 (после `"service_name": "pachka-bot-2"`)
   - Убедитесь, что там стоит `},` (запятая после закрывающей скобки)
   - Если запятой нет, добавьте её
   - Сохраните: `Ctrl+O`, затем `Enter`, затем `Ctrl+X`
   
   **Вариант Б: Использование sed (быстрое исправление):**
   ```bash
   # Проверьте строку 22
   sed -n '22p' bots_config.json
   
   # Если там нет запятой, добавьте её автоматически
   # (заменит "}" на "}," если после bot2 нет запятой)
   sed -i '22s/"service_name": "pachka-bot-2"/"service_name": "pachka-bot-2"/' bots_config.json
   sed -i '22s/}$/},/' bots_config.json
   ```
   
   **Правильная структура должна быть:**
   ```json
   "bot2": {
     ...
     "service_name": "pachka-bot-2"
   },    ← ЗДЕСЬ ДОЛЖНА БЫТЬ ЗАПЯТАЯ!
   "bot3": {
     ...
   }
   ```
   
   **Вариант В: Просмотр проблемной области:**
   ```bash
   # Посмотрите строки 20-25
   sed -n '20,25p' bots_config.json
   ```
   
   Убедитесь, что после строки с `"service_name": "pachka-bot-2"` идет строка с `},` (запятая обязательна!)

4. **Если не получается исправить, скопируйте заново:**
   ```bash
   cp bots_config.example.json bots_config.json
   # Затем заполните своими данными
   ```

### Health endpoint не отвечает или возвращает пустой ответ

Если `curl http://91.217.77.71:5002/health` возвращает пустой ответ или не отвечает:

**Решение:**

1. **Проверьте, запущен ли бот:**
   ```bash
   # Если бот запущен как сервис
   sudo systemctl status pachka-bot-3.service
   ```
   
   **Если сервис не найден (Unit could not be found):**
   
   Сервис еще не создан. Создайте его (см. Шаг 7.1) или запустите бот вручную:
   ```bash
   cd ~/mrnet_bot_sims
   source find_sims_env/bin/activate
   python3 start_bot3.py
   ```
   
   **Если сервис существует, но не запущен:**
   ```bash
   # Запустите бот
   sudo systemctl start pachka-bot-3.service
   sudo systemctl status pachka-bot-3.service
   ```
   
   **Если бот запущен вручную, проверьте процессы:**
   ```bash
   ps aux | grep start_bot3
   ps aux | grep python | grep 5002
   ```

2. **Проверьте, слушается ли порт:**
   ```bash
   netstat -tlnp | grep 5002
   # или
   ss -tlnp | grep 5002
   ```
   
   Если порт не слушается, бот не запущен или не может запуститься.

3. **Проверьте логи на ошибки:**
   ```bash
   # Если бот запущен как сервис
   sudo journalctl -u pachka-bot-3.service -n 100
   
   # Если бот запущен вручную
   tail -n 100 universal_bot.log
   grep -i error universal_bot.log
   ```

4. **Проверьте файрвол:**
   ```bash
   # Для ufw
   sudo ufw status
   sudo ufw allow 5002/tcp
   sudo ufw reload
   
   # Проверьте, что порт открыт
   sudo ufw status | grep 5002
   ```

5. **Попробуйте запустить бот вручную для диагностики:**
   ```bash
   cd ~/mrnet_bot_sims
   source find_sims_env/bin/activate
   python3 start_bot3.py
   ```
   
   Смотрите на вывод - должны быть сообщения о запуске сервера. Если есть ошибки, они будут видны.

6. **Проверьте, не занят ли порт другим процессом:**
   ```bash
   # Найти процесс, использующий порт 5002
   sudo lsof -i :5002
   # или
   sudo fuser 5002/tcp
   ```

7. **Проверьте конфигурацию:**
   ```bash
   # Убедитесь, что в bots_config.json для bot3 указан порт 5002
   grep -A 10 '"bot3"' bots_config.json | grep port
   ```

### Ошибка "ModuleNotFoundError: No module named 'dotenv'"

Эта ошибка означает, что виртуальное окружение не активировано или зависимости не установлены.

**Решение для ручного запуска:**

1. **Активируйте виртуальное окружение:**
   ```bash
   source find_sims_env/bin/activate
   ```
   
   Убедитесь, что в начале строки появилось `(find_sims_env)`.

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Повторите команду:**
   ```bash
   python3 start_bot3.py --check
   ```

**Альтернатива:** Используйте полный путь к Python из виртуального окружения:
```bash
find_sims_env/bin/python start_bot3.py --check
```

**Решение для systemd сервиса:**

Если ошибка возникает при запуске через systemd:

1. **Остановите сервис:**
   ```bash
   sudo systemctl stop pachka-bot-3.service
   ```

2. **Установите зависимости в виртуальное окружение:**
   ```bash
   cd ~/mrnet_bot_sims
   source find_sims_env/bin/activate
   pip install -r requirements.txt
   # Проверьте установку
   pip list | grep dotenv
   deactivate
   ```

3. **Проверьте, что Python из виртуального окружения видит зависимости:**
   ```bash
   /home/titkov/mrnet_bot_sims/find_sims_env/bin/python -c "import dotenv; print('OK')"
   ```
   
   **Если ошибка "ModuleNotFoundError":**
   
   Это означает, что зависимости установлены не в то виртуальное окружение. Исправьте:
   
   ```bash
   # Убедитесь, что вы в правильной директории
   cd ~/mrnet_bot_sims
   
   # Проверьте, какой pip используется
   which pip
   # Должно быть: /home/titkov/mrnet_bot_sims/find_sims_env/bin/pip
   
   # Если путь неправильный, активируйте окружение заново
   source find_sims_env/bin/activate
   
   # Проверьте путь к pip снова
   which pip
   
   # Установите зависимости, используя полный путь к pip
   /home/titkov/mrnet_bot_sims/find_sims_env/bin/pip install -r requirements.txt
   
   # Или используйте python -m pip (более надежно)
   /home/titkov/mrnet_bot_sims/find_sims_env/bin/python -m pip install -r requirements.txt
   
   # Проверьте установку
   /home/titkov/mrnet_bot_sims/find_sims_env/bin/python -c "import dotenv; print('OK')"
   ```
   
   Теперь должно вывести "OK" без ошибок.

4. **Проверьте путь к Python в systemd сервисе:**
   ```bash
   sudo cat /etc/systemd/system/pachka-bot-3.service | grep ExecStart
   ```
   
   Убедитесь, что путь правильный и указывает на Python из виртуального окружения.

5. **Перезапустите сервис:**
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl start pachka-bot-3.service
   sudo systemctl status pachka-bot-3.service
   ```

### Бот не отвечает на команды

1. Проверьте, что webhook настроен правильно в Pachka
2. Проверьте, что URL webhook_outgoing доступен из интернета
3. Проверьте логи бота на наличие ошибок
4. Убедитесь, что токены в `bots_config.json` правильные

### Ошибка "Address already in use" (порт занят)

Если при запуске бота появляется ошибка `[Errno 98] Address already in use`, это означает, что порт 5002 уже занят другим процессом.

**Решение:**

1. **Найдите процесс, использующий порт 5002:**
   ```bash
   # Вариант 1: Используя lsof
   sudo lsof -i :5002
   
   # Вариант 2: Используя fuser
   sudo fuser 5002/tcp
   
   # Вариант 3: Используя netstat
   sudo netstat -tlnp | grep 5002
   
   # Вариант 4: Используя ss
   sudo ss -tlnp | grep 5002
   ```

2. **Остановите процесс, занимающий порт:**
   
   **Если это старый процесс бота:**
   ```bash
   # Найдите PID процесса из вывода выше
   # Затем остановите его
   sudo kill -9 PID
   # или
   pkill -f start_bot3
   pkill -f "python.*5002"
   ```
   
   **Если это другой бот (bot1 или bot2):**
   - Не останавливайте его, если он нужен
   - Измените порт для bot3 в `bots_config.json`

3. **Проверьте, что порт освободился:**
   ```bash
   sudo lsof -i :5002
   # Должно быть пусто
   ```

4. **Запустите бота снова:**
   ```bash
   # Если запускаете вручную
   source find_sims_env/bin/activate
   python3 start_bot3.py
   
   # Если запускаете как сервис
   sudo systemctl start pachka-bot-3.service
   ```

5. **Альтернатива: Измените порт для bot3:**
   
   Если порт нужен другому процессу, измените порт в `bots_config.json`:
   ```json
   "bot3": {
     ...
     "port": 5003,
     "webhook_outgoing": "http://91.217.77.71:5003/webhook",
     ...
   }
   ```
   
   И обновите webhook в Pachka на новый порт: `http://91.217.77.71:5003/webhook`
   
   Не забудьте открыть новый порт в файрволе:
   ```bash
   sudo ufw allow 5003/tcp
   sudo ufw reload
   ```

### Проблемы с портом (общая информация)

Если порт 5002 занят и вы хотите использовать другой порт, измените его в `bots_config.json`:
```json
"port": 5003
```

И обновите webhook_outgoing соответственно, а также настройки webhook в Pachka.

## 📝 Важные замечания

1. **Бот автоматически запускает скрипт экспорта каждый день в 17:00 MSK**
2. **Все три бота могут работать одновременно** - они используют разные порты (5000, 5001, 5002)
3. **Каждый бот должен иметь свой уникальный webhook** в Pachka
4. **Каждый бот должен иметь свой уникальный API токен**
5. **Файлы экспорта доступны для скачивания по HTTP** - ссылки отправляются в сообщениях

## 🚀 Функционал бота 3

### Автоматический запуск скрипта

Бот автоматически запускает скрипт экспорта ICCID:IMEI **каждый день в 17:00 MSK**. 

После выполнения скрипта:
- Создаются CSV файлы в папке `iccid_imei_export/exports/`
- Бот отправляет сообщение с кликабельными ссылками на файлы
- Файлы остаются доступными для скачивания до следующего запуска скрипта
- Старые файлы автоматически удаляются перед созданием новых

### Доступные команды

- `/start` - показать приветственное сообщение с описанием функций
- `/run_script` - запустить скрипт экспорта вручную (не дожидаясь 17:00)

### Скачивание файлов

Файлы доступны для скачивания по HTTP:
- URL: `http://91.217.77.71:5002/files/имя_файла.csv`
- Файлы автоматически удаляются перед следующим запуском скрипта
- В сообщениях бота файлы представлены как кликабельные ссылки

### Настройка Google Sheets

Для работы скрипта экспорта требуется:
1. **Файл `credentials.json`** (или `client_secret.json`) - должен находиться в:
   - `~/mrnet_bot_sims/credentials.json` (рекомендуется)
   - или `~/mrnet_bot_sims/iccid_imei_export/credentials.json`

2. **Файл `token.json`** - создаётся автоматически при первой авторизации:
   - Должен находиться в той же папке, что и `credentials.json`
   - Если файла нет, нужно один раз запустить скрипт вручную с доступом к браузеру для авторизации

**Важно:** На сервере без браузера `token.json` нужно создать вручную на компьютере с браузером, а затем скопировать на сервер.

## 🔄 Обновление и перезапуск бота

Для быстрого обновления кода и перезапуска бота используйте скрипт:

```bash
cd ~/mrnet_bot_sims
./restart_bot3.sh
```

Скрипт автоматически:
1. Обновляет репозиторий (`git pull`)
2. Останавливает сервис
3. Перезапускает сервис
4. Проверяет статус

**Перед первым использованием сделайте скрипт исполняемым:**
```bash
chmod +x restart_bot3.sh
```

## 📁 Структура проекта

```
mrnet_bot_sims/
├── iccid_imei_export/          # Скрипт экспорта ICCID:IMEI
│   ├── main.py                 # Основной скрипт экспорта
│   ├── google_sheets_processor.py
│   ├── exports/               # Папка с экспортированными файлами
│   │   └── *.csv              # CSV файлы (удаляются перед новым запуском)
│   └── requirements.txt
├── llms_bot_pachka/
│   └── universal_bot.py       # Основной код бота
├── start_bot3.py              # Скрипт запуска бота 3
├── restart_bot3.sh            # Скрипт обновления и перезапуска
├── bots_config.json           # Конфигурация ботов
└── credentials.json           # Учётные данные Google API (здесь или в iccid_imei_export/)
```

## 🚀 Следующие шаги

После того, как бот успешно запущен:
- Проверьте работу автоматического запуска в 17:00 MSK
- Протестируйте команду `/run_script` для ручного запуска
- Убедитесь, что файлы доступны для скачивания по ссылкам

