#!/bin/bash

# Скрипт для настройки постоянного виртуального окружения

ENV_NAME="find_sims_env"
ENV_PATH="./$ENV_NAME"

echo "🔧 Настройка виртуального окружения..."

# Проверяем, существует ли уже окружение
if [ ! -d "$ENV_PATH" ]; then
    echo "📦 Создаем новое виртуальное окружение..."
    python3 -m venv "$ENV_PATH"
else
    echo "✅ Виртуальное окружение уже существует"
fi

# Активируем окружение
echo "🔌 Активируем виртуальное окружение..."
source "$ENV_PATH/bin/activate"

# Обновляем pip
echo "⬆️ Обновляем pip..."
pip install --upgrade pip

# Устанавливаем зависимости
echo "📚 Устанавливаем зависимости..."
pip install -r requirements.txt
pip install -r llms_bot_pachka/requirements.txt

echo "✅ Окружение настроено!"
echo "🚀 Для активации выполните: source $ENV_PATH/bin/activate"
echo "🐍 Для запуска: python main.py" 